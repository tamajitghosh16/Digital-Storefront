NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
SUPABASE_SERVICE_ROLE_KEY=
DATABASE_URL=
DIRECT_URL=
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=
RESEND_API_KEY=
# Required for cover/banner image uploads (app/api/uploads). Without it the
# admin's image fields fall back to "use a link instead" and say so.
BLOB_READ_WRITE_TOKEN=
INNGEST_EVENT_KEY=
INNGEST_SIGNING_KEY=
# Where the back office's "View storefront" link points. Defaults to
# http://localhost:3000 when unset.
NEXT_PUBLIC_STOREFRONT_URL=
